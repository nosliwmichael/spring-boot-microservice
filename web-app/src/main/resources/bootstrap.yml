spring:
  application:
    name: "aperture"
  main:
    banner-mode: "off"

---
spring:
  profiless: local, dev
  cloud:
    config:
      uri: http://localhost:8080

---
spring:
  profiles: prod
  cloud:
    config:
      uri: ${config.server.url}